# Copy to .env and fill in before running docker compose up
NGROK_AUTHTOKEN=your_ngrok_auth_token_here

# Optional: pin a static ngrok domain (requires ngrok paid plan).
# Leave blank to get a random ephemeral URL each run.
NGROK_DOMAIN=
