.venv/
venv/
__pycache__/
*.pyc
*.egg-info/
.env
.DS_Store

# Salesforce IDE / CLI local cache
.sfdx/
.sf/
salesforce/.sfdx/
salesforce/.sf/

# Node / Next.js
node_modules/
.next/
frontend/.next/
frontend/node_modules/

# Maven build output
target/

# Editor / tool scratch dirs
.scratch/
.vercel

# Per-target smoke-test reports (only the localhost one is committed
# as SMOKE_TEST_RESULTS.md — see frontend/scripts/smoke-test.mjs).
SMOKE_TEST_RESULTS.*.md

# Build journal artifact — generated by scripts/build_journal.py, lives
# at the repo root, ~90 KB. The script IS versioned; the .docx is not.
Pause-Health.ai-Build-Journal.docx

# Claude Code local state (settings, worktrees, plans) stays local.
# Only the shared dev-preview launch config is versioned. Ignore everything
# under .claude/ EXCEPT launch.json — the un-ignore of the file requires the
# directory itself not be ignored, so ignore its contents (/*) not the dir.
.claude/*
!.claude/launch.json
