{
  "name": "@pause-health/cli",
  "version": "0.1.0",
  "description": "Salesforce-style CLI for Pause-Health.ai's Experience APIs. Wraps the same /api/mulesoft/* surface the MCP server and Care Router consume — so an operator gets the same data via `pause` commands that an agent gets via tools/call.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "bin": {
    "pause": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "dev": "tsx src/cli.ts",
    "smoke": "node scripts/smoke.mjs",
    "test": "vitest run",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=18.17"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
